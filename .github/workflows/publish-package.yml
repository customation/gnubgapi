name: Publish NuGet Package (GitHub Packages)

on:
  workflow_dispatch:
    inputs:
      version_prefix:
        description: "Major.Minor version prefix (e.g. 0.1)"
        required: false
        default: "0.1"
  push:
    branches: [ master ]
    paths:
      - 'src/**'
      - 'runtimes/**'
      - '.github/workflows/publish-package.yml'

concurrency:
  group: nuget-publish-${{ github.ref }}
  cancel-in-progress: false

env:
  VERSION_PREFIX: ${{ github.event.inputs.version_prefix || '0.1' }}
  CONFIGURATION: Release
  DOTNET_VERSION: '10.0.x'

jobs:
  pack-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Verify native binaries
        run: |
          echo "=== Native binaries ==="
          find runtimes -type f -name '*.dll' -o -name '*.so' | while read f; do
            echo "  $f ($(stat --printf='%s' "$f") bytes)"
          done
          # Fail if no native binaries found
          count=$(find runtimes -type f \( -name '*.dll' -o -name '*.so' \) | wc -l)
          if [ "$count" -eq 0 ]; then
            echo "::error::No native binaries found in runtimes/"
            exit 1
          fi

      - name: Compute version
        run: |
          VERSION="${VERSION_PREFIX}.${GITHUB_RUN_NUMBER}"
          echo "PACKAGE_VERSION=$VERSION" >> $GITHUB_ENV
          echo "Package version: $VERSION"

      - name: Create nuget.config
        run: |
          cat > nuget.config <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <packageSources>
              <add key="github" value="https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" />
            </packageSources>
            <packageSourceCredentials>
              <github>
                <add key="Username" value="${{ github.repository_owner }}" />
                <add key="ClearTextPassword" value="${{ secrets.GITHUB_TOKEN }}" />
              </github>
            </packageSourceCredentials>
          </configuration>
          EOF

      - name: Pack
        run: |
          dotnet pack src/GammonBase.GnuBgApi/GammonBase.GnuBgApi.csproj \
            -c $CONFIGURATION \
            -p:PackageVersion=$PACKAGE_VERSION \
            -p:ContinuousIntegrationBuild=true \
            -o artifacts
          ls -la artifacts/

      - name: Push to GitHub Packages
        run: |
          for nupkg in artifacts/*.nupkg; do
            echo "Pushing $nupkg"
            dotnet nuget push "$nupkg" \
              --source "github" \
              --api-key ${{ secrets.GITHUB_TOKEN }} \
              --skip-duplicate
          done

      - name: Summary
        run: |
          echo "Published version: $PACKAGE_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Native binaries included:" >> $GITHUB_STEP_SUMMARY
          find runtimes -type f -name '*.dll' -o -name '*.so' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY
